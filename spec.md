# 作業日報アプリ 開発仕様書

## 1. 文書情報

* 仮称：作業日報帳
* 開発方式：Claude Codeを使用
* 初期形態：スマートフォン・パソコン対応Webアプリ
* 開発段階：MVP（最小実用版）
* 作成日：2026年8月8日

---

# 2. アプリ概要

## 2.1 目的

日々の作業時間（開始・終了時刻）と作業内容、その日の時間単価を記録し、作業名（作業の種類）ごとに費用を集計する。作業によって単価が異なることを前提とし、毎月21日〜翌月20日を1つの締め期間として、期間内の累計費用をいつでも確認できるようにする。

## 2.2 アプリの特徴

* 1日ごとに「開始時刻」「終了時刻」「作業内容」「時間単価」を記録する
* 時間単価は日ごと・記録ごとに毎回入力する（作業名によって単価が異なるため、固定値にしない）
* 複数の作業名を登録し、日報をそれぞれに紐づけて管理する
* 作業時間は「開始〜終了の時間から、勤務時間帯が12:00〜13:00と重なる場合のみ、昼休憩1時間を差し引いた時間」とする（午前のみ・午後のみの勤務では休憩を差し引かない）
* 1日8時間を超えた作業時間には、超過分に残業割増（1.25倍）を自動で適用する
* 土曜日の作業は単価に1.25倍、日曜日の作業は単価に1.35倍の割増を自動で適用する
* 土日の残業（8時間超）は、曜日の割増と残業の割増を掛け合わせて計算する（例：土曜日の残業分＝単価×1.25×1.25）
* 毎月21日〜翌月20日を1つの締め期間とし、期間ごとの累計金額を自動集計する
* 個人利用を前提とし、複数人での共有・請求書発行機能は初期版には含めない

## 2.3 想定利用者

初期版は開発者本人が、業務委託・フリーランス的な働き方における日々の稼働記録・請求金額の把握のために使用する。

* 複数の作業（案件・業務内容）ごとに時間単価が異なる働き方をしている人
* 毎月決まった締め日で報酬を計算する必要がある人
* 日々の作業内容も一緒に記録しておきたい人

---

# 3. 開発方針

## 3.1 初期版の考え方

初期版では、会員登録・複数ユーザー対応・請求書発行機能は実装しない。まず本人が実際の業務で使用し、必要な項目・不要な項目を見極めながら仕様を調整する。

## 3.2 MVPに含める機能

* 作業名の登録・編集・削除
* 日報（作業時間・作業内容・時間単価）の登録・編集・削除
* 作業時間・金額の自動計算（時間単価 × 作業時間）
* 1日8時間超の残業割増（1.25倍）の自動計算
* 土曜日1.25倍・日曜日1.35倍の曜日割増の自動計算（残業との掛け合わせを含む）
* 締め期間（21日〜翌月20日）ごとの自動集計
* 締め期間・作業名ごとの費用サマリー表示
* 日報一覧（期間・作業名での絞り込み）
* CSV・JSON形式でのデータ書き出し
* 締め期間の日報一覧のPDF出力（宛先・会社名・氏名の設定画面付き）
* データのバックアップと復元

## 3.3 初期版に含めない機能

* 複数人での共有・権限管理
* 請求書・納品書の自動発行
* 休憩時間の柔軟な入力（初期版は「12:00〜13:00と重なる場合のみ1時間差し引く」という自動判定のみとし、休憩時間そのものを毎回手入力する機能は将来検討）
* 深夜（22時〜翌5時等）の割増単価の自動判定（将来検討）
* 祝日の割増判定（初期版は曜日のみで判定し、祝日は考慮しない。将来検討）
* 日をまたぐ勤務（終了時刻が翌日になるケース）の自動対応（将来検討、初期版は同日内の時刻のみ）
* 外部会計ソフトとの連携
* 通貨換算・複数通貨対応
* 自動リマインド通知

---

# 4. 基本的な利用の流れ

1. アプリを開く
2. （初回のみ）作業名を登録する
3. 毎日の作業終了後、日報を入力する（日付、作業名、開始時刻、終了時刻、作業内容、時間単価）
4. 入力すると、その日の作業時間と金額が自動計算されて表示される
5. ホーム画面で、今の締め期間の累計金額（全体・作業名別）を確認する
6. 締め日が近づいたら、締め期間集計画面で確定額を確認し、必要に応じてCSVで書き出す

---

# 5. 画面構成

## 5.1 画面一覧

| 画面ID | 画面名        | 主な役割                        |
| ---- | ---------- | --------------------------- |
| S01  | ホーム画面      | 今日の入力状況、今締め期間の累計金額サマリーを表示    |
| S02  | 日報入力画面     | 日報の新規登録・編集                   |
| S03  | 日報一覧画面     | 日報を一覧・絞り込み表示                 |
| S04  | 日報詳細画面     | 1件の日報の詳細確認・編集・削除             |
| S05  | 作業名一覧画面    | 作業名の一覧と締め期間内の金額サマリー   |
| S06  | 作業名入力画面    | 作業名の新規登録・編集           |
| S07  | 締め期間集計画面   | 指定した締め期間の合計金額・内訳を表示          |
| S08  | 設定画面       | 締め日、表示設定、データ管理               |
| S09  | データ管理画面    | 書き出し、バックアップ、復元               |
| S10  | ヘルプ・バージョン情報画面 | 簡単な使い方の説明とアプリのバージョン表示 |
| S11  | PDF出力設定画面 | PDF出力時のヘッダー情報（宛先・会社名・氏名）の登録・編集 |

---

## 5.2 S01 ホーム画面

### 表示内容

* 今日の日付
* 今日の日報が未入力の場合、「今日の日報を入力する」ボタンを目立たせて表示
* 今日の日報が入力済みの場合、その内容（作業名・作業時間・金額）を表示
* 今の締め期間（例：「2026年8月度（7/21〜8/20）」）の累計金額（全体合計）
* 今の締め期間の作業名別内訳（簡易リスト）
* 今の締め期間の合計作業時間
* 締め日までの残り日数
* 「日報一覧を見る」「作業名一覧を見る」への導線

### 表示例

「2026年8月度（7/21〜8/20）の累計は 128,000円（32.0時間）です。締め日まであと5日です。」

---

## 5.3 S02 日報入力画面

### 入力項目

* 日付（必須、デフォルトは今日）
* 作業名（必須、登録済みの一覧から選択）
* 開始時刻（必須。設定画面の「基本の開始時刻」、または直前に入力した開始時刻を初期値として提案する。当日入力時に自由に変更可能）
* 終了時刻（必須。初期値は18:00とする。設定画面の「基本の終了時刻」で変更可能。当日入力時に自由に変更可能）
* 作業内容（自由記入、必須）
* 時間単価（必須、金額。直前に同じ作業名で入力した単価を初期値として提案する）
* メモ（自由記入、任意）

### 自動計算・表示

* 拘束時間 = 終了時刻 − 開始時刻（時間単位、小数第2位まで）
* 昼休憩の判定：勤務時間帯（開始時刻〜終了時刻）が「12:00〜13:00」と重なっている場合のみ、休憩時間を1.0時間とする。重なっていない場合（例：終了が12:00以前、または開始が13:00以降）は、休憩時間を0時間とする（詳しい判定方法は「9.4 昼休憩・残業・曜日割増の計算仕様」を参照）
* 作業時間 = 拘束時間 − 休憩時間（上記で判定した0時間または1.0時間）
* 曜日区分 = 作業日の曜日から自動判定（平日／土曜／日曜）
* 通常時間 = 作業時間のうち8時間までの部分
* 残業時間 = 作業時間のうち8時間を超えた部分
* 金額 = （通常時間 × 時間単価 × 曜日倍率）＋（残業時間 × 時間単価 × 曜日倍率 × 1.25）
  * 曜日倍率：平日=1.0、土曜=1.25、日曜=1.35
  * 詳しい計算方法は「9.4 昼休憩・残業・曜日割増の計算仕様」を参照
* 入力中の画面にリアルタイムで「拘束時間」「昼休憩の有無」「作業時間（うち通常◯時間・残業◯時間）」「適用される割増（曜日・残業）」「本日の金額」を表示する

### 入力チェック

* 終了時刻は開始時刻より後でなければならない（同日内のみ対応、日をまたぐ入力はエラーとして案内する）
* 昼休憩が適用される（12:00〜13:00と重なる）勤務で、拘束時間が1時間以下の場合、休憩を差し引くと作業時間が0以下になるため、エラーメッセージ（例：「休憩時間を差し引くと作業時間が0以下になります。時刻を確認してください」）を表示し、登録できないようにする
* 時間単価は0より大きい数値
* 同じ日付・同じ作業名の日報が既に存在する場合は、上書き確認を表示する

---

## 5.4 S03 日報一覧画面

### 一覧表示内容

* 日付
* 作業名
* 開始〜終了時刻
* 作業時間
* 時間単価
* 金額
* 作業内容（先頭部分）

### 並び替え

* 日付順（新しい順・古い順）
* 作業名別
* 金額順

### 絞り込み

* 締め期間（今期間／前期間／任意の期間を指定）
* 作業名
* キーワード検索（作業内容・メモ）

---

## 5.5 S04 日報詳細画面

### 表示内容

* 日報の全項目
* 計算された作業時間・金額

### 操作

* 編集
* 削除（確認ダイアログあり）

削除確認文例：

「この日報を削除します。元に戻せません。」

---

## 5.6 S05 作業名一覧画面

### 一覧表示内容

* 作業名
* 今締め期間の累計金額・累計時間
* 直近の作業日
* 登録している基本単価（設定していれば参考表示）

### 並び替え

* 累計金額順
* 名前順
* 最終作業日順

---

## 5.7 S06 作業名入力画面

### 入力項目

* 作業名（必須）
* 基本の時間単価（任意、日報入力時の初期値として使用。あくまで参考値で、日報ごとに変更可能）
* メモ・契約内容など（自由記入、任意）
* ステータス（使用中／休止中／終了）

---

## 5.8 S07 締め期間集計画面

### 表示内容

* 期間選択（前月・今月・任意の期間を「◯年◯月度」の形式で選択）
* 選択した期間の合計金額・合計作業時間
* 作業名別の内訳（金額・時間・日報件数）
* 日別の内訳（簡易カレンダーまたはリスト表示）
* CSV書き出しボタン（この期間の日報のみを対象に出力）
* PDF出力ボタン（この期間の日報のみを対象に出力。仕様は5.13参照）

### 期間の考え方

締め期間は「21日始まり・翌月20日締め」とする。表示ラベルは、締め日（20日）が属する月を基準に「◯月度」と呼ぶ。

例：7月21日〜8月20日の期間 →「8月度」

---

## 5.9 S08 設定画面

### 設定項目

* 表示名
* 締め開始日（初期値：21日、将来的に変更できるようにしておく）
* 基本の開始時刻（日報入力時の初期値、任意設定）
* 基本の終了時刻（日報入力時の初期値、初期値：18:00）
* 通貨単位（初期は円固定、表示のみ）
* 時刻の入力形式（24時間表記固定）
* 日報一覧の初期並び順
* テーマ設定
* データ保存方式

---

## 5.10 S09 データ管理画面

### 機能

* CSV書き出し（全期間、または締め期間を指定して出力）
* PDF出力（締め期間を指定して出力。全期間は対象外。仕様は5.13参照）
* JSONバックアップ（全データを一括保存）
* JSONから復元
* 全データ削除
* バックアップ日時の表示

全データ削除時は、確認文を入力させる。

---

## 5.11 S10 ヘルプ・バージョン情報画面

初めて使う人（アルバイトさん等）でも迷わず操作できるよう、簡単な使い方とアプリのバージョン情報をまとめて表示する画面。設定画面から導線を設ける。

### 表示内容

**使い方セクション**（見出しごとに区切って表示、画像は使わずテキストのみで簡潔に）

1. **作業名を登録する**：まず「作業名」画面から、担当する作業の名前と時給を登録してください。
2. **日報を入力する**：毎日の作業が終わったら、「日報を入力」から記録してください。
   * 日付：作業した日を選びます（通常は今日のままでOK）
   * 作業名：登録した作業名から選びます
   * 開始時刻：作業を始めた時刻を入力します
   * 終了時刻：作業を終えた時刻を入力します
   * 作業内容：どんな作業をしたか簡単に書きます
   * 時間単価：作業名を選ぶと自動で時給が入ります（変更もできます）
3. **金額の自動計算について**：入力すると、その日の作業時間と金額が自動で計算されます。1日8時間を超えた分や、土曜日・日曜日の作業は、割増の金額が自動で加算されます。
4. **締め期間について**：毎月21日〜翌月20日が1つの区切り（締め期間）です。ホーム画面で、今の締め期間の合計金額を確認できます。
5. **データを送るとき**：設定画面の「データ管理」から、CSVまたはJSON形式で書き出して、担当者に送ってください。
6. **日報をPDFで提出するとき**：先に「PDF出力設定」で宛先・会社名・氏名を登録してください。その後、「締め期間集計」画面または「データ管理」画面で月度（例：8/21〜9/20の期間は「9月度」）を選び、「PDF出力」ボタンを押すと、A4サイズの業務日報PDFが保存されます。

**バージョン情報セクション**

* アプリ名：作業日報帳
* バージョン番号（例：v1.2.0、更新のたびに担当者が更新する）
* 最終更新日
* 開発：Claude Codeで作成

**更新履歴セクション**

* バージョンごとに、更新日と更新内容を新しい順に表示する

| バージョン | 更新日 | 更新内容 |
| ------ | ---------- | ---- |
| v1.0.5 | 2026-09-23 | PDF出力で過去の月度を選択できる機能を追加 |
| v1.0.4 | 2026-09-23 | 締め期間の日報一覧をPDF出力する機能（宛先・会社名・氏名の設定画面付き）を追加 |
| v1.0.3 | 2026-09-18 | 日報・作業名の保存時にクラウドへ自動バックアップする機能を追加 |
| v1.0.2 | 2026-08-22 | 昼休憩の差し引きを、勤務時間帯が12:00〜13:00と重なる場合のみに変更 |
| v1.0.1 | 2026-08-12 | 終了時刻の初期値を18:00にし、基本の終了時刻の設定を追加 |
| v1.0.0 | 2026-08-11 | ヘルプ・バージョン情報画面を追加 |

### 操作

* 設定画面から「ヘルプ」を開く導線を設ける
* ヘルプ画面から「PDF出力設定」画面を開く導線を設ける
* ホーム画面の初回起動時（データが1件もない状態）にも、ヘルプ画面への案内を軽く表示する

---

## 5.12 S11 PDF出力設定画面

PDF出力時にヘッダーへ記載する情報を登録・編集する画面。設定画面およびヘルプ・バージョン情報画面から遷移できる。

### 設定項目

* 宛先（取引先名など。例：「○○様作業」。複数登録できる。入力した文字列をそのままPDFに記載する）
* 会社名
* 氏名

### 操作

* 宛先は「追加」ボタンで行を増やし、各行の「削除」ボタンで取り除ける
* 「保存する」ボタンで保存する。保存内容は端末内（IndexedDB）に保持し、JSONバックアップ・復元の対象に含める
* 一度保存した内容は、PDF出力のたびに自動でヘッダーへ反映される
* すべて任意項目とし、未入力の項目はPDF上で空欄とする

---

## 5.13 PDF出力仕様

### 出力範囲・操作

* CSV出力と同じ「締め期間（21日〜翌月20日）」単位で出力する
* 締め期間集計画面（S07）では、選択中の期間を「PDF出力」ボタンで出力する
* データ管理画面（S09）では、月度を選択して「PDFを書き出す」ボタンで出力する
* 宛先が2件以上登録されている場合は、出力時に宛先を選択するプルダウンを表示する（1件以下の場合は表示しない）
* 期間内の日報が0件の場合はボタンを無効にする
* ファイル名：「◯年◯月度_作業日報.pdf」（選択した月度を含める。例：「2026年9月度_作業日報.pdf」）
* PDFはブラウザ内で生成し、サーバーへは送信しない

### 月度選択

* 月度の定義：締め日（20日）が属する月を月度とする（例：8/21〜9/20の期間 →「9月度」）。ホーム画面・締め期間集計画面と同じルール
* 締め期間集計画面（S07）：画面上部の期間選択（今月・前月・プルダウン）で選んだ月度をPDF出力する
* データ管理画面（S09）：PDF書き出し欄のプルダウンで月度を選ぶ。選択肢は「現在の月度」と「日報データが存在するすべての月度」を新しい順に並べ、「2026年9月度（8/21〜9/20）」の形式で表示する。日報データが存在する限り、過去の月度をさかのぼって選択できる
* 初期選択は現在（直近）の月度とする
* 選択した月度に応じて、PDFのタイトル（「◯月 業務日報」）・対象期間・明細データ・合計・ファイル名が切り替わる
* 選択した月度に日報が1件もない場合は、出力ボタンを無効にし「この期間の日報はありません。」と表示する

### レイアウト（A4縦・印刷してそのまま提出・保存できる体裁）

* **タイトル**：「◯月 業務日報」（◯は締め期間の月＝締め日の属する月）
* **ヘッダー情報**：
  * 左上：宛先（大きめの文字、下線付き）
  * 右上：作成日（出力日、「YYYY年M月D日」）、年（締め期間の年、「YYYY年」）、会社名、氏名
  * 期間：「対象期間：YYYY年M月D日〜YYYY年M月D日」
* **明細テーブル**（締め期間内の全日報を、日付・開始時刻順に1日報1行で表示）：

| 列 | 内容 |
| ---- | ---- |
| № | 連番（1から） |
| 日付 | 作業日（「M/D」） |
| 曜日 | 作業日の曜日（「月」「火」…）。土曜・日曜は色分けする |
| 業務内容 | 作業名 |
| 摘要 | 「開始～終了 作業内容 作業時間」をまとめて記載（例：「9:00～18:00 亘理工場 8時間」）。作業場所等は日報の「作業内容」欄の値を用いる |
| 単価 | 時間単価（円） |
| 金額 | 金額（円、残業・曜日割増を含む計算済みの値） |

* 摘要が長い場合は列幅に合わせて折り返す
* **合計行**：テーブル末尾に「合計」として、期間内の合計作業時間（摘要列）・合計金額（金額列）を記載する
* **ページ番号**：各ページ下部中央に「ページ番号 / 総ページ数」を記載する。行数が1ページに収まらない場合は自動で改ページし、各ページにテーブル見出し行を再掲する
* **フォント**：日本語を正しく表示するため、日本語フォント（BIZ UDゴシック、SIL Open Font License）をPDFに埋め込む（使用文字のみのサブセット埋め込み）

---

# 6. 機能一覧

| 機能ID | 機能名        | 内容                          | 優先度 |
| ---- | ---------- | --------------------------- | --- |
| F01  | 作業名登録      | 作業名を新規登録する            | 必須  |
| F02  | 作業名編集・削除   | 作業名情報を修正・削除する               | 必須  |
| F03  | 日報登録       | 1日分の作業記録を登録する               | 必須  |
| F04  | 日報編集・削除    | 日報の内容を修正・削除する               | 必須  |
| F05  | 作業時間自動計算   | 開始〜終了時刻から作業時間を計算する          | 必須  |
| F06  | 金額自動計算     | 作業時間 × 時間単価で金額を計算する         | 必須  |
| F07  | 締め期間判定     | 日付から所属する締め期間（21日〜翌月20日）を判定する | 必須  |
| F08  | 締め期間別集計    | 期間ごとの合計金額・時間を計算する           | 必須  |
| F09  | 作業名別集計     | 作業名ごとの累計金額・時間を計算する          | 必須  |
| F10  | ホームサマリー    | 今締め期間の累計・締め日までの残り日数を表示する    | 必須  |
| F11  | 日報一覧・絞込    | 期間・作業名・キーワードで絞り込む           | 必須  |
| F12  | 単価初期値提案    | 同じ作業名の直近単価を入力時に提案する         | 推奨  |
| F13  | 開始・終了時刻初期値提案 | 開始時刻は基本値/直前入力、終了時刻は18:00を初期値として提案する | 推奨  |
| F14  | CSV出力      | 日報データを期間指定で出力する             | 必須  |
| F15  | JSONバックアップ | 全データをJSONで保存する              | 必須  |
| F16  | JSON復元     | バックアップから復元する                | 必須  |
| F17  | PWA化       | ホーム画面への追加に対応する              | 推奨  |
| F18  | 締め日カスタマイズ  | 締め開始日を21日以外に変更できるようにする      | 将来  |
| F19  | 請求書発行      | 締め期間の内容から請求書を生成する           | 将来  |
| F20  | 日またぎ対応     | 終了時刻が翌日になる勤務に対応する           | 将来  |
| F21  | ヘルプ表示      | 簡単な使い方の説明を画面内で確認できる         | 必須  |
| F22  | バージョン情報表示  | アプリのバージョン番号・最終更新日・更新履歴を表示する      | 必須  |
| F23  | PDF出力      | 締め期間の日報一覧を業務日報形式のPDFで出力する     | 必須  |
| F24  | PDF出力設定    | PDFヘッダーの宛先（複数可）・会社名・氏名を登録する   | 必須  |

---

# 7. データ項目

## 7.1 作業名テーブル

テーブル名：`work_types`

| 項目名          | データ型 | 必須 | 内容                |
| ------------ | ---- | -- | ----------------- |
| id           | 文字列  | 必須 | 作業名固有ID            |
| name         | 文字列  | 必須 | 作業名        |
| default_rate | 数値   | 任意 | 基本の時間単価（入力時の初期値用）  |
| status       | 文字列  | 必須 | 使用中／休止中／終了         |
| memo         | 文字列  | 任意 | メモ・契約内容            |
| created_at   | 日時   | 必須 | 登録日時               |
| updated_at   | 日時   | 必須 | 更新日時               |

## 7.2 日報テーブル

テーブル名：`work_logs`

| 項目名          | データ型 | 必須 | 内容                        |
| ------------ | ---- | -- | ------------------------- |
| id           | 文字列  | 必須 | 日報固有ID                    |
| work_type_id    | 文字列  | 必須 | 作業名ID              |
| work_date    | 日付   | 必須 | 作業日                       |
| start_time   | 時刻   | 必須 | 開始時刻                      |
| end_time     | 時刻   | 必須 | 終了時刻                      |
| raw_duration | 数値   | 必須 | 拘束時間（終了−開始、休憩差し引き前、自動計算）  |
| break_hours  | 数値   | 必須 | 休憩時間（12:00〜13:00と勤務時間帯が重なる場合は1.0、重ならない場合は0。自動判定） |
| work_hours   | 数値   | 必須 | 作業時間（自動計算、raw_duration − break_hours） |
| day_type     | 文字列  | 必須 | 曜日区分（平日／土曜／日曜、work_dateから自動判定） |
| day_multiplier | 数値 | 必須 | 曜日倍率（平日1.0／土曜1.25／日曜1.35、自動設定） |
| regular_hours | 数値  | 必須 | 通常時間（作業時間のうち8時間までの部分、自動計算） |
| overtime_hours | 数値 | 必須 | 残業時間（作業時間のうち8時間を超えた部分、自動計算） |
| hourly_rate  | 数値   | 必須 | この日報の時間単価                 |
| amount       | 数値   | 必須 | 金額（自動計算、通常分＋残業分の合計。計算式は9.4参照） |
| content      | 文字列  | 必須 | 作業内容                      |
| memo         | 文字列  | 任意 | メモ                        |
| period_label | 文字列  | 必須 | 所属する締め期間のラベル（例："2026-08"、自動算出） |
| created_at   | 日時   | 必須 | 登録日時                      |
| updated_at   | 日時   | 必須 | 更新日時                      |

## 7.3 設定テーブル

テーブル名：`app_settings`

| 項目名                 | データ型 | 内容                    |
| ------------------- | ---- | --------------------- |
| display_name        | 文字列  | 表示名                   |
| closing_start_day   | 数値   | 締め開始日（初期値：21）          |
| default_start_time  | 時刻   | 基本の開始時刻（日報入力時の初期値、任意設定） |
| default_end_time    | 時刻   | 基本の終了時刻（日報入力時の初期値、初期値：18:00） |
| default_sort        | 文字列  | 日報一覧の初期並び順            |
| theme               | 文字列  | 画面テーマ                 |
| storage_mode        | 文字列  | 保存方式                  |
| updated_at          | 日時   | 設定更新日時                |
| app_version         | 文字列  | アプリのバージョン番号（コード内に定数として保持し、ヘルプ画面に表示） |
| pdf_recipients      | 文字列配列 | PDF出力の宛先一覧（複数登録可） |
| pdf_company_name    | 文字列  | PDF出力の会社名 |
| pdf_person_name     | 文字列  | PDF出力の氏名 |

---

# 8. 入力チェック

## 8.1 日報

* 作業名：必須（未登録の場合は先に作業名登録を促す）
* 開始時刻・終了時刻：必須、終了時刻は開始時刻より後であること（同日内のみ、日またぎはエラーメッセージを表示し初期版では非対応と案内）
* 昼休憩が適用される場合（勤務時間帯が12:00〜13:00と重なる場合）、拘束時間が1時間以下だと作業時間が0以下になるためエラーとし、登録できないようにする（12:00〜13:00と重ならない勤務ではこのチェックは適用しない）
* 作業内容：必須、1〜200文字
* 時間単価：必須、0より大きい数値
* 同一日付・同一作業名の日報が既に存在する場合、上書き確認を表示する（1日に複数作業名の作業がある場合は、作業名ごとに別の日報として複数登録可能とする）

## 8.2 作業名

* 作業名：必須、1〜100文字
* 基本の時間単価：0以上の数値、未入力可

---

# 9. 締め期間の計算仕様

## 9.1 締め期間の判定ロジック

締め開始日（初期値：21日）を基準に、以下のルールで日付を締め期間に振り分ける。

* 対象日の「日」が21日以上の場合：`対象日の月の21日` 〜 `翌月の20日` の期間に属する
* 対象日の「日」が20日以下の場合：`前月の21日` 〜 `対象日の月の20日` の期間に属する

## 9.2 期間ラベルの付け方

締め日（20日）が属する年月を基準に「◯年◯月度」とする。

例：

* 2026年7月21日〜2026年8月20日 →「2026年8月度」
* 2026年8月21日〜2026年9月20日 →「2026年9月度」

`period_label` はデータ保存時に自動算出し、`YYYY-MM` の形式（締め月基準）で日報に保存しておく。これにより、期間ごとの集計時に再計算せず高速に絞り込める。

## 9.3 集計計算

* 期間合計金額 = その期間に属する日報の `amount` の合計
* 期間合計時間 = その期間に属する日報の `work_hours`（休憩差し引き後の作業時間）の合計
* 作業名別内訳 = 期間内の日報を `work_type_id` ごとにグループ化して集計

## 9.4 昼休憩・残業・曜日割増の計算仕様

### 昼休憩（12:00〜13:00）の判定

勤務時間帯（開始時刻〜終了時刻）が、休憩時間帯「12:00〜13:00」と重なっているかどうかで、休憩時間（`break_hours`）を判定する。

```
重なっている（開始時刻 < 13:00 かつ 終了時刻 > 12:00）→ break_hours = 1.0
重なっていない → break_hours = 0
```

判定例：

* 開始9:00・終了18:00（お昼をまたぐ）→ 重なっている → 休憩1.0時間差し引き
* 開始9:00・終了12:00（お昼ちょうどに終了）→ 重なっていない（終了時刻が12:00と等しく、12:00より後ではないため）→ 休憩なし
* 開始13:00・終了18:00（午後のみ）→ 重なっていない → 休憩なし
* 開始8:00・終了12:30（お昼に少しかかる）→ 重なっている → 休憩1.0時間差し引き

作業時間 = 拘束時間 − break_hours

### 曜日区分と倍率

作業日（`work_date`）の曜日から自動的に判定する。

| 曜日区分 | 倍率 |
| ---- | --- |
| 平日（月〜金） | 1.0 |
| 土曜日 | 1.25 |
| 日曜日 | 1.35 |

### 残業（8時間超）の判定

* 通常時間 = `min(work_hours, 8)`
* 残業時間 = `max(work_hours − 8, 0)`

### 金額の計算式

```
通常分の金額 = 通常時間 × 時間単価 × 曜日倍率
残業分の金額 = 残業時間 × 時間単価 × 曜日倍率 × 1.25
金額（amount） = 通常分の金額 + 残業分の金額
```

土日の残業は、曜日倍率と残業倍率（1.25）を掛け合わせる。例えば土曜日の残業分は「単価 × 1.25（土曜）× 1.25（残業）＝単価 × 1.5625」となる。

### 計算例

以下の例はすべて、お昼休憩（12:00〜13:00）をまたぐ勤務であることを前提とし、「◯時間勤務」は休憩差し引き後の作業時間（`work_hours`）を指す。

* **平日・8時間勤務・単価1,500円**：通常8時間のみ。8 × 1,500 × 1.0 ＝ 12,000円
* **平日・10時間勤務・単価1,500円**：通常8時間＋残業2時間。(8 × 1,500 × 1.0) + (2 × 1,500 × 1.0 × 1.25) ＝ 12,000円 + 3,750円 ＝ 15,750円
* **土曜日・8時間勤務・単価1,500円**：通常8時間のみ、曜日倍率1.25。8 × 1,500 × 1.25 ＝ 15,000円
* **土曜日・10時間勤務・単価1,500円**：通常8時間＋残業2時間、いずれも曜日倍率1.25適用。(8 × 1,500 × 1.25) + (2 × 1,500 × 1.25 × 1.25) ＝ 15,000円 + 4,687.5円 ＝ 19,687.5円
* **日曜日・10時間勤務・単価1,500円**：通常8時間＋残業2時間、いずれも曜日倍率1.35適用。(8 × 1,500 × 1.35) + (2 × 1,500 × 1.35 × 1.25) ＝ 16,200円 + 5,062.5円 ＝ 21,262.5円
* **午後のみ・13:00〜18:00勤務・単価1,500円**：12:00〜13:00と重ならないため休憩差し引きなし。拘束時間5時間＝作業時間5時間（通常のみ）。5 × 1,500 × 1.0 ＝ 7,500円

### 表示上の注意

日報入力画面・詳細画面では、「通常時間・残業時間」「適用された曜日倍率」を内訳として表示し、金額の根拠が利用者にも分かるようにする。金額に端数（円未満）が出る場合は、四捨五入して整数円で保存・表示する。

---

# 10. UI・操作性

## 10.1 基本方針

* スマートフォンを縦向きで使いやすくする
* 日々の入力（日報登録）を最短の手数で行えるようにする
* 金額は桁区切りカンマ付きで見やすく表示する
* 締め期間の切り替えは分かりやすいタブまたはドロップダウンで行う

## 10.2 推奨文字サイズ

* 通常本文：16px以上
* 金額表示：20px以上、太字
* 見出し：18px以上

## 10.3 ボタン・操作

* 高さ44px以上
* 「削除」操作には必ず確認を入れる
* 時刻入力は、スマートフォンのネイティブな時刻ピッカーを活用する

---

# 11. データ保存とプライバシー

## 11.1 初期版

初期版は、ブラウザ内のローカルデータベース（IndexedDB）に保存する。外部サーバーへ自動送信しない。

## 11.2 注意点

報酬に直結するデータのため、JSONバックアップ機能を早い段階で用意し、締め日ごとにバックアップを取ることを推奨する。

## 11.3 将来版

複数端末での利用や、請求書発行と連携させる場合は、クラウド保存（Supabase等）を検討する。

---

# 12. 推奨技術構成

* フロントエンド：Next.js
* 開発言語：TypeScript
* デザイン：Tailwind CSS
* データ保存：IndexedDB
* IndexedDB操作：Dexie.js
* 入力検証：Zod
* 日付・時刻処理：date-fns
* グラフ・内訳表示：Recharts（必要に応じて）
* アプリ形式：PWA対応
* ソース管理：GitHub

---

# 13. 開発ステップ

## 第1段階：基本の日報・作業名管理

* プロジェクト作成
* 作業名の登録・編集・削除
* 日報の登録・編集・削除
* 作業時間・金額の自動計算
* 日報一覧画面（基本表示）

## 第2段階：締め期間集計

* 締め期間の自動判定ロジック実装
* ホーム画面での今期間サマリー表示
* 締め期間集計画面（期間選択・作業名別内訳）
* 作業名一覧画面での期間内累計表示

## 第3段階：使いやすさ強化

* 単価の初期値提案（直近単価を記憶）
* 日報一覧の絞り込み・検索強化
* 締め日までの残り日数表示

## 第4段階：データ管理・PWA化

* CSV出力（期間指定）
* JSONバックアップ・復元
* PWA化（ホーム画面への追加対応）

## 第5段階：将来拡張

* 締め開始日のカスタマイズ機能
* 請求書発行機能
* 日またぎ勤務への対応
* 割増単価（深夜・休日）の自動判定

---

# 14. 完成条件

初期版は、次の条件を満たした時点で完成とする。

1. スマートフォンとパソコンで画面が崩れない
2. 作業名を登録・編集・削除できる
3. 日報（日付・作業名・開始終了時刻・作業内容・時間単価）を登録・編集・削除できる
4. 作業時間と金額が自動計算される
5. 締め期間（21日〜翌月20日）が正しく判定され、期間ごとの合計が確認できる
6. ホーム画面で今締め期間の累計金額・締め日までの残り日数が分かる
7. 作業名ごとの累計金額が確認できる
8. 日報一覧を期間・作業名で絞り込める
9. CSV形式で期間を指定して書き出せる
10. JSON形式でバックアップと復元ができる
11. ページを閉じてもデータが残る
12. 削除の前に確認が表示される
13. スマートフォンのホーム画面に追加できる（PWA）

---

# 15. 開発上の重要事項

* 日々の入力のしやすさを最優先する（毎日使うことを想定）
* 時間単価は固定値にせず、日報ごとに毎回入力・調整できるようにする
* 昼休憩の判定（12:00〜13:00と重なるかどうか）は間違えやすい部分なので、境界値（終了がちょうど12:00、開始がちょうど13:00の場合など）を含めてテストする
* 残業（8時間超）・曜日割増（土曜1.25倍／日曜1.35倍）・両者の掛け合わせ計算は金額に直結するため、9.4の計算例を用いて必ずテストする
* 締め期間の判定ロジックは特にテストを重点的に行う（月またぎ・年またぎのケースを確認する）
* 報酬に関わるデータのため、金額計算の正確性を最優先する
* バックアップ機能を早い段階で実装する

---

# 16. Claude Codeへの初回開発指示

以下の仕様で、スマートフォンとパソコンに対応したPWA形式のWebアプリを作成してください。

アプリ名は「作業日報帳」です。Next.js、TypeScript、Tailwind CSSを使用してください。データはIndexedDBへ保存し、Dexie.jsで操作してください。

最初の開発範囲は次のとおりです（第1段階：基本の日報・作業名管理）。

1. ホーム画面（簡易版：今日の入力状況表示）
2. 作業名一覧画面
3. 作業名入力・編集画面
4. 日報一覧画面
5. 日報入力・編集画面（開始・終了時刻から作業時間、時間単価から金額を自動計算して画面にリアルタイム表示）
6. 日報詳細画面

日報の入力項目は、日付（必須）、作業名（必須、登録済み一覧から選択）、開始時刻（必須）、終了時刻（必須）、作業内容（必須）、時間単価（必須）、メモです。作業名の入力項目は、作業名（必須）、基本の時間単価（任意、日報入力時の初期値に使う）、メモ、ステータスです。

休憩時間は「昼休憩1時間」を固定で差し引く仕様としてください（終了時刻−開始時刻の拘束時間から、常に1.0時間を差し引いたものを作業時間とする）。拘束時間が1時間以下の場合は、作業時間が0以下になるためエラーとして登録できないようにしてください。終了時刻が開始時刻より前になる入力（日またぎ）は、初期版ではエラーとして扱ってください。

締め期間（21日〜翌月20日）の自動判定・集計機能は、第1段階の完成後、別途追加で依頼します。まずは日報と作業名の基本的なCRUD（登録・編集・削除・一覧表示）と、作業時間・金額の自動計算を確実に動く形で実装してください。

コードは機能ごとに分割し、データ型、入力検証、エラー処理を実装してください。最初に全体のフォルダ構成と作業手順を提示し、その後、段階的に実装してください。
