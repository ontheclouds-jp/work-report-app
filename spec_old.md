# 作業日報アプリ 開発仕様書

## 1. 文書情報

* 仮称：作業日報帳
* 開発方式：Claude Codeを使用
* 初期形態：スマートフォン・パソコン対応Webアプリ
* 開発段階：MVP（最小実用版）
* 作成日：2026年8月8日

---

# 2. アプリ概要

## 2.1 目的

日々の作業時間（開始・終了時刻）と作業内容、その日の時間単価を記録し、複数の取引先・プロジェクトごとに費用を集計する。毎月21日〜翌月20日を1つの締め期間として、期間内の累計費用をいつでも確認できるようにする。

## 2.2 アプリの特徴

* 1日ごとに「開始時刻」「終了時刻」「作業内容」「時間単価」を記録する
* 時間単価は日ごと・記録ごとに毎回入力する（取引先によって単価が異なるため、固定値にしない）
* 複数の取引先・プロジェクトを登録し、日報をそれぞれに紐づけて管理する
* 作業時間は「開始〜終了の時間から、昼休憩1時間を固定で差し引いた時間」とする
* 毎月21日〜翌月20日を1つの締め期間とし、期間ごとの累計金額を自動集計する
* 個人利用を前提とし、複数人での共有・請求書発行機能は初期版には含めない

## 2.3 想定利用者

初期版は開発者本人が、業務委託・フリーランス的な働き方における日々の稼働記録・請求金額の把握のために使用する。

* 複数の取引先から時間単価制で仕事を受けている人
* 毎月決まった締め日で報酬を計算する必要がある人
* 日々の作業内容も一緒に記録しておきたい人

---

# 3. 開発方針

## 3.1 初期版の考え方

初期版では、会員登録・複数ユーザー対応・請求書発行機能は実装しない。まず本人が実際の業務で使用し、必要な項目・不要な項目を見極めながら仕様を調整する。

## 3.2 MVPに含める機能

* 取引先・プロジェクトの登録・編集・削除
* 日報（作業時間・作業内容・時間単価）の登録・編集・削除
* 作業時間・金額の自動計算（時間単価 × 作業時間）
* 締め期間（21日〜翌月20日）ごとの自動集計
* 締め期間・取引先ごとの費用サマリー表示
* 日報一覧（期間・取引先での絞り込み）
* CSV・JSON形式でのデータ書き出し
* データのバックアップと復元

## 3.3 初期版に含めない機能

* 複数人での共有・権限管理
* 請求書・納品書の自動発行
* 休憩時間の柔軟な入力（初期版は昼休憩1時間の固定差し引きのみとし、休憩時間そのものを毎回入力する機能は将来検討）
* 深夜・休日等の割増単価の自動判定
* 日をまたぐ勤務（終了時刻が翌日になるケース）の自動対応（将来検討、初期版は同日内の時刻のみ）
* 外部会計ソフトとの連携
* 通貨換算・複数通貨対応
* 自動リマインド通知

---

# 4. 基本的な利用の流れ

1. アプリを開く
2. （初回のみ）取引先・プロジェクトを登録する
3. 毎日の作業終了後、日報を入力する（日付、取引先、開始時刻、終了時刻、作業内容、時間単価）
4. 入力すると、その日の作業時間と金額が自動計算されて表示される
5. ホーム画面で、今の締め期間の累計金額（全体・取引先別）を確認する
6. 締め日が近づいたら、締め期間集計画面で確定額を確認し、必要に応じてCSVで書き出す

---

# 5. 画面構成

## 5.1 画面一覧

| 画面ID | 画面名        | 主な役割                        |
| ---- | ---------- | --------------------------- |
| S01  | ホーム画面      | 今日の入力状況、今締め期間の累計金額サマリーを表示    |
| S02  | 日報入力画面     | 日報の新規登録・編集                   |
| S03  | 日報一覧画面     | 日報を一覧・絞り込み表示                 |
| S04  | 日報詳細画面     | 1件の日報の詳細確認・編集・削除             |
| S05  | 取引先一覧画面    | 取引先・プロジェクトの一覧と締め期間内の金額サマリー   |
| S06  | 取引先入力画面    | 取引先・プロジェクトの新規登録・編集           |
| S07  | 締め期間集計画面   | 指定した締め期間の合計金額・内訳を表示          |
| S08  | 設定画面       | 締め日、表示設定、データ管理               |
| S09  | データ管理画面    | 書き出し、バックアップ、復元               |

---

## 5.2 S01 ホーム画面

### 表示内容

* 今日の日付
* 今日の日報が未入力の場合、「今日の日報を入力する」ボタンを目立たせて表示
* 今日の日報が入力済みの場合、その内容（取引先・作業時間・金額）を表示
* 今の締め期間（例：「2026年8月度（7/21〜8/20）」）の累計金額（全体合計）
* 今の締め期間の取引先別内訳（簡易リスト）
* 今の締め期間の合計作業時間
* 締め日までの残り日数
* 「日報一覧を見る」「取引先一覧を見る」への導線

### 表示例

「2026年8月度（7/21〜8/20）の累計は 128,000円（32.0時間）です。締め日まであと5日です。」

---

## 5.3 S02 日報入力画面

### 入力項目

* 日付（必須、デフォルトは今日）
* 取引先・プロジェクト（必須、登録済みの一覧から選択）
* 開始時刻（必須）
* 終了時刻（必須）
* 作業内容（自由記入、必須）
* 時間単価（必須、金額。直前に同じ取引先で入力した単価を初期値として提案する）
* メモ（自由記入、任意）

### 自動計算・表示

* 拘束時間 = 終了時刻 − 開始時刻（時間単位、小数第2位まで）
* 作業時間 = 拘束時間 − 1.0時間（昼休憩の固定差し引き）
* 金額 = 作業時間 × 時間単価
* 入力中の画面にリアルタイムで「拘束時間」「休憩差し引き後の作業時間」「本日の金額」を表示する

### 入力チェック

* 終了時刻は開始時刻より後でなければならない（同日内のみ対応、日をまたぐ入力はエラーとして案内する）
* 拘束時間が1時間以下の場合、昼休憩を差し引くと作業時間が0以下になるため、エラーメッセージ（例：「拘束時間が1時間以下のため、休憩を差し引いた作業時間を計算できません」）を表示し、登録できないようにする
* 時間単価は0より大きい数値
* 同じ日付・同じ取引先の日報が既に存在する場合は、上書き確認を表示する

---

## 5.4 S03 日報一覧画面

### 一覧表示内容

* 日付
* 取引先・プロジェクト名
* 開始〜終了時刻
* 作業時間
* 時間単価
* 金額
* 作業内容（先頭部分）

### 並び替え

* 日付順（新しい順・古い順）
* 取引先別
* 金額順

### 絞り込み

* 締め期間（今期間／前期間／任意の期間を指定）
* 取引先・プロジェクト
* キーワード検索（作業内容・メモ）

---

## 5.5 S04 日報詳細画面

### 表示内容

* 日報の全項目
* 計算された作業時間・金額

### 操作

* 編集
* 削除（確認ダイアログあり）

削除確認文例：

「この日報を削除します。元に戻せません。」

---

## 5.6 S05 取引先一覧画面

### 一覧表示内容

* 取引先・プロジェクト名
* 今締め期間の累計金額・累計時間
* 直近の作業日
* 登録している基本単価（設定していれば参考表示）

### 並び替え

* 累計金額順
* 名前順
* 最終作業日順

---

## 5.7 S06 取引先入力画面

### 入力項目

* 取引先・プロジェクト名（必須）
* 基本の時間単価（任意、日報入力時の初期値として使用。あくまで参考値で、日報ごとに変更可能）
* メモ・契約内容など（自由記入、任意）
* ステータス（取引中／休止中／終了）

---

## 5.8 S07 締め期間集計画面

### 表示内容

* 期間選択（前月・今月・任意の期間を「◯年◯月度」の形式で選択）
* 選択した期間の合計金額・合計作業時間
* 取引先別の内訳（金額・時間・日報件数）
* 日別の内訳（簡易カレンダーまたはリスト表示）
* CSV書き出しボタン（この期間の日報のみを対象に出力）

### 期間の考え方

締め期間は「21日始まり・翌月20日締め」とする。表示ラベルは、締め日（20日）が属する月を基準に「◯月度」と呼ぶ。

例：7月21日〜8月20日の期間 →「8月度」

---

## 5.9 S08 設定画面

### 設定項目

* 表示名
* 締め開始日（初期値：21日、将来的に変更できるようにしておく）
* 通貨単位（初期は円固定、表示のみ）
* 時刻の入力形式（24時間表記固定）
* 日報一覧の初期並び順
* テーマ設定
* データ保存方式

---

## 5.10 S09 データ管理画面

### 機能

* CSV書き出し（全期間、または締め期間を指定して出力）
* JSONバックアップ（全データを一括保存）
* JSONから復元
* 全データ削除
* バックアップ日時の表示

全データ削除時は、確認文を入力させる。

---

# 6. 機能一覧

| 機能ID | 機能名        | 内容                          | 優先度 |
| ---- | ---------- | --------------------------- | --- |
| F01  | 取引先登録      | 取引先・プロジェクトを新規登録する            | 必須  |
| F02  | 取引先編集・削除   | 取引先情報を修正・削除する               | 必須  |
| F03  | 日報登録       | 1日分の作業記録を登録する               | 必須  |
| F04  | 日報編集・削除    | 日報の内容を修正・削除する               | 必須  |
| F05  | 作業時間自動計算   | 開始〜終了時刻から作業時間を計算する          | 必須  |
| F06  | 金額自動計算     | 作業時間 × 時間単価で金額を計算する         | 必須  |
| F07  | 締め期間判定     | 日付から所属する締め期間（21日〜翌月20日）を判定する | 必須  |
| F08  | 締め期間別集計    | 期間ごとの合計金額・時間を計算する           | 必須  |
| F09  | 取引先別集計     | 取引先ごとの累計金額・時間を計算する          | 必須  |
| F10  | ホームサマリー    | 今締め期間の累計・締め日までの残り日数を表示する    | 必須  |
| F11  | 日報一覧・絞込    | 期間・取引先・キーワードで絞り込む           | 必須  |
| F12  | 単価初期値提案    | 同じ取引先の直近単価を入力時に提案する         | 推奨  |
| F13  | CSV出力      | 日報データを期間指定で出力する             | 必須  |
| F14  | JSONバックアップ | 全データをJSONで保存する              | 必須  |
| F15  | JSON復元     | バックアップから復元する                | 必須  |
| F16  | PWA化       | ホーム画面への追加に対応する              | 推奨  |
| F17  | 締め日カスタマイズ  | 締め開始日を21日以外に変更できるようにする      | 将来  |
| F18  | 請求書発行      | 締め期間の内容から請求書を生成する           | 将来  |
| F19  | 日またぎ対応     | 終了時刻が翌日になる勤務に対応する           | 将来  |

---

# 7. データ項目

## 7.1 取引先テーブル

テーブル名：`clients`

| 項目名          | データ型 | 必須 | 内容                |
| ------------ | ---- | -- | ----------------- |
| id           | 文字列  | 必須 | 取引先固有ID            |
| name         | 文字列  | 必須 | 取引先・プロジェクト名        |
| default_rate | 数値   | 任意 | 基本の時間単価（入力時の初期値用）  |
| status       | 文字列  | 必須 | 取引中／休止中／終了         |
| memo         | 文字列  | 任意 | メモ・契約内容            |
| created_at   | 日時   | 必須 | 登録日時               |
| updated_at   | 日時   | 必須 | 更新日時               |

## 7.2 日報テーブル

テーブル名：`work_logs`

| 項目名          | データ型 | 必須 | 内容                        |
| ------------ | ---- | -- | ------------------------- |
| id           | 文字列  | 必須 | 日報固有ID                    |
| client_id    | 文字列  | 必須 | 取引先・プロジェクトID              |
| work_date    | 日付   | 必須 | 作業日                       |
| start_time   | 時刻   | 必須 | 開始時刻                      |
| end_time     | 時刻   | 必須 | 終了時刻                      |
| raw_duration | 数値   | 必須 | 拘束時間（終了−開始、休憩差し引き前、自動計算）  |
| break_hours  | 数値   | 必須 | 休憩時間（初期版は固定1.0、将来の可変対応に備えて保存） |
| work_hours   | 数値   | 必須 | 作業時間（自動計算、raw_duration − break_hours） |
| hourly_rate  | 数値   | 必須 | この日報の時間単価                 |
| amount       | 数値   | 必須 | 金額（自動計算、work_hours × hourly_rate） |
| content      | 文字列  | 必須 | 作業内容                      |
| memo         | 文字列  | 任意 | メモ                        |
| period_label | 文字列  | 必須 | 所属する締め期間のラベル（例："2026-08"、自動算出） |
| created_at   | 日時   | 必須 | 登録日時                      |
| updated_at   | 日時   | 必須 | 更新日時                      |

## 7.3 設定テーブル

テーブル名：`app_settings`

| 項目名                 | データ型 | 内容                    |
| ------------------- | ---- | --------------------- |
| display_name        | 文字列  | 表示名                   |
| closing_start_day   | 数値   | 締め開始日（初期値：21）          |
| default_sort        | 文字列  | 日報一覧の初期並び順            |
| theme               | 文字列  | 画面テーマ                 |
| storage_mode        | 文字列  | 保存方式                  |
| updated_at          | 日時   | 設定更新日時                |

---

# 8. 入力チェック

## 8.1 日報

* 取引先：必須（未登録の場合は先に取引先登録を促す）
* 開始時刻・終了時刻：必須、終了時刻は開始時刻より後であること（同日内のみ、日またぎはエラーメッセージを表示し初期版では非対応と案内）
* 拘束時間（終了−開始）が1時間以下の場合、昼休憩1時間を差し引くと作業時間が0以下になるためエラーとし、登録できないようにする
* 作業内容：必須、1〜200文字
* 時間単価：必須、0より大きい数値
* 同一日付・同一取引先の日報が既に存在する場合、上書き確認を表示する（1日に複数取引先の作業がある場合は、取引先ごとに別の日報として複数登録可能とする）

## 8.2 取引先

* 取引先名：必須、1〜100文字
* 基本の時間単価：0以上の数値、未入力可

---

# 9. 締め期間の計算仕様

## 9.1 締め期間の判定ロジック

締め開始日（初期値：21日）を基準に、以下のルールで日付を締め期間に振り分ける。

* 対象日の「日」が21日以上の場合：`対象日の月の21日` 〜 `翌月の20日` の期間に属する
* 対象日の「日」が20日以下の場合：`前月の21日` 〜 `対象日の月の20日` の期間に属する

## 9.2 期間ラベルの付け方

締め日（20日）が属する年月を基準に「◯年◯月度」とする。

例：

* 2026年7月21日〜2026年8月20日 →「2026年8月度」
* 2026年8月21日〜2026年9月20日 →「2026年9月度」

`period_label` はデータ保存時に自動算出し、`YYYY-MM` の形式（締め月基準）で日報に保存しておく。これにより、期間ごとの集計時に再計算せず高速に絞り込める。

## 9.3 集計計算

* 期間合計金額 = その期間に属する日報の `amount` の合計
* 期間合計時間 = その期間に属する日報の `work_hours`（昼休憩1時間差し引き後の作業時間）の合計
* 取引先別内訳 = 期間内の日報を `client_id` ごとにグループ化して集計

---

# 10. UI・操作性

## 10.1 基本方針

* スマートフォンを縦向きで使いやすくする
* 日々の入力（日報登録）を最短の手数で行えるようにする
* 金額は桁区切りカンマ付きで見やすく表示する
* 締め期間の切り替えは分かりやすいタブまたはドロップダウンで行う

## 10.2 推奨文字サイズ

* 通常本文：16px以上
* 金額表示：20px以上、太字
* 見出し：18px以上

## 10.3 ボタン・操作

* 高さ44px以上
* 「削除」操作には必ず確認を入れる
* 時刻入力は、スマートフォンのネイティブな時刻ピッカーを活用する

---

# 11. データ保存とプライバシー

## 11.1 初期版

初期版は、ブラウザ内のローカルデータベース（IndexedDB）に保存する。外部サーバーへ自動送信しない。

## 11.2 注意点

報酬に直結するデータのため、JSONバックアップ機能を早い段階で用意し、締め日ごとにバックアップを取ることを推奨する。

## 11.3 将来版

複数端末での利用や、請求書発行と連携させる場合は、クラウド保存（Supabase等）を検討する。

---

# 12. 推奨技術構成

* フロントエンド：Next.js
* 開発言語：TypeScript
* デザイン：Tailwind CSS
* データ保存：IndexedDB
* IndexedDB操作：Dexie.js
* 入力検証：Zod
* 日付・時刻処理：date-fns
* グラフ・内訳表示：Recharts（必要に応じて）
* アプリ形式：PWA対応
* ソース管理：GitHub

---

# 13. 開発ステップ

## 第1段階：基本の日報・取引先管理

* プロジェクト作成
* 取引先の登録・編集・削除
* 日報の登録・編集・削除
* 作業時間・金額の自動計算
* 日報一覧画面（基本表示）

## 第2段階：締め期間集計

* 締め期間の自動判定ロジック実装
* ホーム画面での今期間サマリー表示
* 締め期間集計画面（期間選択・取引先別内訳）
* 取引先一覧画面での期間内累計表示

## 第3段階：使いやすさ強化

* 単価の初期値提案（直近単価を記憶）
* 日報一覧の絞り込み・検索強化
* 締め日までの残り日数表示

## 第4段階：データ管理・PWA化

* CSV出力（期間指定）
* JSONバックアップ・復元
* PWA化（ホーム画面への追加対応）

## 第5段階：将来拡張

* 締め開始日のカスタマイズ機能
* 請求書発行機能
* 日またぎ勤務への対応
* 割増単価（深夜・休日）の自動判定

---

# 14. 完成条件

初期版は、次の条件を満たした時点で完成とする。

1. スマートフォンとパソコンで画面が崩れない
2. 取引先・プロジェクトを登録・編集・削除できる
3. 日報（日付・取引先・開始終了時刻・作業内容・時間単価）を登録・編集・削除できる
4. 作業時間と金額が自動計算される
5. 締め期間（21日〜翌月20日）が正しく判定され、期間ごとの合計が確認できる
6. ホーム画面で今締め期間の累計金額・締め日までの残り日数が分かる
7. 取引先ごとの累計金額が確認できる
8. 日報一覧を期間・取引先で絞り込める
9. CSV形式で期間を指定して書き出せる
10. JSON形式でバックアップと復元ができる
11. ページを閉じてもデータが残る
12. 削除の前に確認が表示される
13. スマートフォンのホーム画面に追加できる（PWA）

---

# 15. 開発上の重要事項

* 日々の入力のしやすさを最優先する（毎日使うことを想定）
* 時間単価は固定値にせず、日報ごとに毎回入力・調整できるようにする
* 昼休憩1時間の差し引き計算は間違えやすい部分なので、境界値（拘束時間がちょうど1時間の場合など）を含めてテストする
* 締め期間の判定ロジックは特にテストを重点的に行う（月またぎ・年またぎのケースを確認する）
* 報酬に関わるデータのため、金額計算の正確性を最優先する
* バックアップ機能を早い段階で実装する

---

# 16. Claude Codeへの初回開発指示

以下の仕様で、スマートフォンとパソコンに対応したPWA形式のWebアプリを作成してください。

アプリ名は「作業日報帳」です。Next.js、TypeScript、Tailwind CSSを使用してください。データはIndexedDBへ保存し、Dexie.jsで操作してください。

最初の開発範囲は次のとおりです（第1段階：基本の日報・取引先管理）。

1. ホーム画面（簡易版：今日の入力状況表示）
2. 取引先一覧画面
3. 取引先入力・編集画面
4. 日報一覧画面
5. 日報入力・編集画面（開始・終了時刻から作業時間、時間単価から金額を自動計算して画面にリアルタイム表示）
6. 日報詳細画面

日報の入力項目は、日付（必須）、取引先（必須、登録済み一覧から選択）、開始時刻（必須）、終了時刻（必須）、作業内容（必須）、時間単価（必須）、メモです。取引先の入力項目は、取引先名（必須）、基本の時間単価（任意、日報入力時の初期値に使う）、メモ、ステータスです。

休憩時間は「昼休憩1時間」を固定で差し引く仕様としてください（終了時刻−開始時刻の拘束時間から、常に1.0時間を差し引いたものを作業時間とする）。拘束時間が1時間以下の場合は、作業時間が0以下になるためエラーとして登録できないようにしてください。終了時刻が開始時刻より前になる入力（日またぎ）は、初期版ではエラーとして扱ってください。

締め期間（21日〜翌月20日）の自動判定・集計機能は、第1段階の完成後、別途追加で依頼します。まずは日報と取引先の基本的なCRUD（登録・編集・削除・一覧表示）と、作業時間・金額の自動計算を確実に動く形で実装してください。

コードは機能ごとに分割し、データ型、入力検証、エラー処理を実装してください。最初に全体のフォルダ構成と作業手順を提示し、その後、段階的に実装してください。
